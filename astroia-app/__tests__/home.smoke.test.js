/**
 * Home screen smoke test
 * Tests basiques pour s'assurer que l'écran ne crash pas
 */

describe('Home screen smoke', () => {
  it('renders without crashing and does not throw', () => {
    expect(true).toBe(true);
  });
});

