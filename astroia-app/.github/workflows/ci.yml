name: CI - Astro.IA

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read
  pull-requests: write


jobs:
  lint-and-typecheck:
    name: Lint & TypeCheck
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📥 Install dependencies
        run: npm ci

      - name: 🔍 Run ESLint
        run: npm run lint
        continue-on-error: true

      - name: 🔎 Run TypeScript check
        run: npm run typecheck

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: lint-and-typecheck
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📥 Install dependencies
        run: npm ci

      - name: 🧪 Run tests with coverage
        run: npm run test:ci

      - name: 📊 Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/lcov.info
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false

      - name: 📈 Comment PR with coverage
        if: github.event_name == 'pull_request'
        uses: romeovs/lcov-reporter-action@v0.3.1
        with:
          lcov-file: ./coverage/lcov.info
          github-token: ${{ secrets.GITHUB_TOKEN }}

  # Job E2E commenté pour activation future
  # e2e-tests:
  #   name: E2E Tests (Maestro)
  #   runs-on: macos-latest
  #   needs: test
  #   
  #   steps:
  #     - name: 📥 Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: 📦 Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #
  #     - name: 📥 Install dependencies
  #       run: npm ci
  #
  #     - name: ☕ Setup Java (for Android)
  #       uses: actions/setup-java@v4
  #       with:
  #         distribution: 'temurin'
  #         java-version: '17'
  #
  #     - name: 🔧 Setup EAS CLI
  #       run: npm install -g eas-cli
  #
  #     - name: 📱 Setup iOS Simulator
  #       run: |
  #         xcrun simctl create "iPhone 15" com.apple.CoreSimulator.SimDeviceType.iPhone-15
  #         xcrun simctl boot "iPhone 15"
  #
  #     - name: 🏗️ Build development app (iOS)
  #       run: |
  #         eas build --profile development --platform ios --local --non-interactive
  #       env:
  #         EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
  #
  #     - name: 🎭 Install Maestro
  #       run: |
  #         curl -Ls "https://get.maestro.mobile.dev" | bash
  #         echo "${HOME}/.maestro/bin" >> $GITHUB_PATH
  #
  #     - name: 🧪 Run Maestro E2E tests
  #       run: |
  #         maestro test .maestro/ \
  #           --format junit \
  #           --output test-results/maestro/
  #
  #     - name: 📊 Upload E2E test results
  #       if: always()
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: maestro-test-results
  #         path: test-results/maestro/
  #
  #     - name: 📸 Upload screenshots
  #       if: always()
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: maestro-screenshots
  #         path: ~/.maestro/tests/

  build-preview:
    name: Build Preview (EAS)
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'pull_request'
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📥 Install dependencies
        run: npm ci

      - name: 🔧 Setup Expo
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 🏗️ Build preview (commented for now)
        run: echo "Preview build would run here with eas build --profile preview"
        # Uncomment when ready:
        # run: eas build --profile preview --platform all --non-interactive

  sentry-release:
    name: Create Sentry Release
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🚀 Create Sentry release
        uses: getsentry/action-release@v1
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
          SENTRY_PROJECT: astro-ia-app
        with:
          environment: production
          version: ${{ github.sha }}

