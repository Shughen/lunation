# Configuration Gitleaks pour Astroia Lunar
# Détection de secrets hardcodés

[extend]
# Utiliser la configuration par défaut de gitleaks
useDefault = true

[allowlist]
# Patterns autorisés (exemples, templates)
regexes = [
    '''sk-ant-example''',
    '''ANTHROPIC_API_KEY=sk-ant-\.\.\.''',
]
