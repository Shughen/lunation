[pytest]
# Configuration pytest-asyncio pour une meilleure isolation des tests
asyncio_mode = auto

# Marqueurs personnalisés
markers =
    serial: marque les tests qui doivent s'exécuter séquentiellement
    real_db: tests qui utilisent la vraie DB (Postgres CI)

